# Max file size.
spring.servlet.multipart.max-file-size=5MB

# Max request size.
spring.servlet.multipart.max-request-size=10MB

# Define the persistent H2 database
# spring.datasource.url=jdbc:h2:~/test;DB_CLOSE_ON_EXIT=FALSE
